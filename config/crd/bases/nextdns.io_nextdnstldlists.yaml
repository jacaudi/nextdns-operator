---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: nextdnstldlists.nextdns.io
spec:
  group: nextdns.io
  names:
    kind: NextDNSTLDList
    listKind: NextDNSTLDListList
    plural: nextdnstldlists
    singular: nextdnstldlist
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.tldCount
      name: TLDs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NextDNSTLDList is the Schema for the nextdnstldlists API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NextDNSTLDListSpec defines the desired state of NextDNSTLDList
            properties:
              description:
                description: Description provides context for this TLD list
                type: string
              tlds:
                description: TLDs is the list of top-level domains to block
                items:
                  description: TLDEntry represents a TLD in the block list
                  properties:
                    active:
                      default: true
                      description: Active indicates if this TLD is blocked
                      type: boolean
                    reason:
                      description: Reason documents why this TLD is blocked
                      type: string
                    tld:
                      description: |-
                        TLD is the top-level domain (without the dot)
                        Examples: "com", "net", "co.uk"
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?(\.[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)*$
                      type: string
                  required:
                  - tld
                  type: object
                minItems: 1
                type: array
            required:
            - tlds
            type: object
          status:
            description: NextDNSTLDListStatus defines the observed state of NextDNSTLDList
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              profileRefs:
                description: ProfileRefs lists profiles using this TLD list
                items:
                  description: ResourceReference identifies a Kubernetes resource
                  properties:
                    name:
                      description: Name of the resource
                      type: string
                    namespace:
                      description: Namespace of the resource (optional, defaults to
                        same namespace)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              tldCount:
                description: TLDCount is the number of active TLDs
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
