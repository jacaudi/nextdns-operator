---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nextdnsprofiles.nextdns.io
spec:
  group: nextdns.io
  names:
    kind: NextDNSProfile
    listKind: NextDNSProfileList
    plural: nextdnsprofiles
    singular: nextdnsprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.profileID
      name: Profile ID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NextDNSProfile is the Schema for the nextdnsprofiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NextDNSProfileSpec defines the desired state of NextDNSProfile
            properties:
              allowlist:
                description: Allowlist specifies inline domains to allow (merged with
                  AllowlistRefs)
                items:
                  description: DomainEntry represents a domain in allow/deny lists
                  properties:
                    active:
                      default: true
                      description: Active indicates if this entry is enabled
                      type: boolean
                    domain:
                      description: Domain is the domain name (supports wildcards like
                        *.example.com)
                      maxLength: 253
                      minLength: 1
                      pattern: ^(\*\.)?([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                      type: string
                    reason:
                      description: Reason documents why this domain is in the list
                      type: string
                  required:
                  - domain
                  type: object
                type: array
              allowlistRefs:
                description: |-
                  AllowlistRefs references NextDNSAllowlist resources
                  Domains from all referenced allowlists are merged
                items:
                  description: ListReference references a list CRD (allowlist, denylist,
                    or TLD list)
                  properties:
                    name:
                      description: Name of the list resource
                      type: string
                    namespace:
                      description: Namespace of the list resource (defaults to profile's
                        namespace)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              configMapRef:
                description: ConfigMapRef configures optional ConfigMap creation with
                  connection details
                properties:
                  enabled:
                    default: false
                    description: Enabled enables creation of the ConfigMap
                    type: boolean
                  name:
                    description: |-
                      Name is the name of the ConfigMap to create
                      If not specified, defaults to "<profile-name>-nextdns"
                    type: string
                type: object
              credentialsRef:
                description: CredentialsRef references a Secret containing the NextDNS
                  API key
                properties:
                  key:
                    default: api-key
                    description: Key is the key within the Secret
                    type: string
                  name:
                    description: Name is the name of the Secret
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the Secret
                      If not set, defaults to the namespace of the referencing resource
                    type: string
                required:
                - name
                type: object
              denylist:
                description: Denylist specifies inline domains to block (merged with
                  DenylistRefs)
                items:
                  description: DomainEntry represents a domain in allow/deny lists
                  properties:
                    active:
                      default: true
                      description: Active indicates if this entry is enabled
                      type: boolean
                    domain:
                      description: Domain is the domain name (supports wildcards like
                        *.example.com)
                      maxLength: 253
                      minLength: 1
                      pattern: ^(\*\.)?([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                      type: string
                    reason:
                      description: Reason documents why this domain is in the list
                      type: string
                  required:
                  - domain
                  type: object
                type: array
              denylistRefs:
                description: |-
                  DenylistRefs references NextDNSDenylist resources
                  Domains from all referenced denylists are merged
                items:
                  description: ListReference references a list CRD (allowlist, denylist,
                    or TLD list)
                  properties:
                    name:
                      description: Name of the list resource
                      type: string
                    namespace:
                      description: Namespace of the list resource (defaults to profile's
                        namespace)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              mode:
                default: managed
                description: |-
                  Mode controls whether the operator manages or only observes this profile
                  In "observe" mode, the operator reads the remote profile into status without modifying it
                  In "managed" mode (default), the operator syncs spec to the remote profile
                enum:
                - observe
                - managed
                type: string
              name:
                description: Name is the human-readable name shown in NextDNS dashboard
                maxLength: 100
                type: string
              parentalControl:
                description: |-
                  ParentalControl configures content filtering.
                  Omitting this section leaves remote parental control settings unchanged.
                properties:
                  blockBypass:
                    default: false
                    description: BlockBypass prevents bypassing parental controls
                    type: boolean
                  categories:
                    description: Categories specifies content categories to block
                    items:
                      description: CategoryEntry references a content category
                      properties:
                        active:
                          default: true
                          description: Active indicates if this category is blocked
                          type: boolean
                        id:
                          description: ID is the category identifier (e.g., "gambling",
                            "adult", "violence")
                          type: string
                        recreation:
                          default: false
                          description: |-
                            Recreation indicates if this category allows recreation time exceptions.
                            Note: Observe mode reads this from the API. Managed mode write support is deferred.
                          type: boolean
                      required:
                      - id
                      type: object
                    type: array
                  safeSearch:
                    default: false
                    description: SafeSearch enforces safe search on search engines
                    type: boolean
                  services:
                    description: Services specifies specific services to block
                    items:
                      description: ServiceEntry references a specific service
                      properties:
                        active:
                          default: true
                          description: Active indicates if this service is blocked
                          type: boolean
                        id:
                          description: ID is the service identifier (e.g., "tiktok",
                            "youtube", "facebook")
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                  youtubeRestrictedMode:
                    default: false
                    description: YouTubeRestrictedMode enforces YouTube restricted
                      mode
                    type: boolean
                type: object
              privacy:
                description: |-
                  Privacy configures tracker and ad blocking.
                  Omitting this section leaves remote privacy settings unchanged.
                properties:
                  allowAffiliate:
                    default: false
                    description: AllowAffiliate allows affiliate & tracking links
                    type: boolean
                  blocklists:
                    description: Blocklists specifies which ad/tracker blocklists
                      to enable
                    items:
                      description: BlocklistEntry references a privacy blocklist
                      properties:
                        active:
                          default: true
                          description: Active indicates if this blocklist is enabled
                          type: boolean
                        id:
                          description: ID is the blocklist identifier (e.g., "nextdns-recommended",
                            "oisd")
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                  disguisedTrackers:
                    default: true
                    description: DisguisedTrackers blocks trackers using CNAME cloaking
                    type: boolean
                  natives:
                    description: Natives specifies native tracking protection (per-vendor)
                    items:
                      description: NativeEntry configures native tracker blocking
                        for a vendor
                      properties:
                        active:
                          default: true
                          description: Active indicates if blocking is enabled for
                            this vendor
                          type: boolean
                        id:
                          description: ID is the vendor identifier (e.g., "apple",
                            "windows", "samsung")
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                type: object
              profileID:
                description: |-
                  ProfileID optionally specifies an existing NextDNS profile to manage
                  If not set, a new profile will be created
                type: string
              rewrites:
                description: |-
                  Rewrites specifies DNS rewrites.
                  Omitting this field leaves remote rewrites unchanged.
                  Setting an empty list explicitly clears all remote rewrites.
                items:
                  description: RewriteEntry defines a DNS rewrite rule
                  properties:
                    active:
                      default: true
                      description: Active indicates if this rewrite is enabled
                      type: boolean
                    from:
                      description: From is the source domain
                      type: string
                    to:
                      description: To is the target (IP or domain)
                      type: string
                  required:
                  - from
                  - to
                  type: object
                type: array
              security:
                description: |-
                  Security configures threat protection settings.
                  Omitting this section leaves remote security settings unchanged.
                properties:
                  aiThreatDetection:
                    default: true
                    description: AIThreatDetection enables AI-based threat detection
                    type: boolean
                  cryptojacking:
                    default: true
                    description: Cryptojacking blocks cryptomining scripts
                    type: boolean
                  csam:
                    default: true
                    description: CSAM blocks child sexual abuse material
                    type: boolean
                  ddns:
                    default: false
                    description: DDNS blocks dynamic DNS hostnames
                    type: boolean
                  dga:
                    default: true
                    description: DGA blocks algorithmically-generated domains
                    type: boolean
                  dnsRebinding:
                    default: true
                    description: DNSRebinding protects against DNS rebinding attacks
                    type: boolean
                  googleSafeBrowsing:
                    default: true
                    description: GoogleSafeBrowsing enables Google Safe Browsing protection
                    type: boolean
                  idnHomographs:
                    default: true
                    description: IDNHomographs blocks IDN homograph attacks
                    type: boolean
                  nrd:
                    default: false
                    description: NRD blocks newly registered domains
                    type: boolean
                  parking:
                    default: true
                    description: Parking blocks parked domains
                    type: boolean
                  threatIntelligenceFeeds:
                    default: true
                    description: ThreatIntelligenceFeeds enables threat intelligence
                      feeds
                    type: boolean
                  typosquatting:
                    default: true
                    description: Typosquatting blocks typosquatting domains
                    type: boolean
                type: object
              settings:
                description: |-
                  Settings configures logging, performance, and other options.
                  Omitting this section leaves remote settings unchanged.
                properties:
                  blockPage:
                    description: BlockPage configures the block page
                    properties:
                      enabled:
                        default: true
                        description: Enabled shows a block page instead of failing
                          silently
                        type: boolean
                    type: object
                  logs:
                    description: Logs configures query logging
                    properties:
                      enabled:
                        default: true
                        description: Enabled turns logging on/off
                        type: boolean
                      location:
                        description: |-
                          Location specifies the log storage location (e.g., "eu", "us", "ch").
                          Valid values depend on the NextDNS plan and may change over time.
                        type: string
                      logClientsIPs:
                        default: false
                        description: LogClientsIPs logs client IP addresses
                        type: boolean
                      logDomains:
                        default: true
                        description: LogDomains logs queried domains
                        type: boolean
                      retention:
                        default: 7d
                        description: Retention specifies log retention period
                        enum:
                        - 1h
                        - 6h
                        - 1d
                        - 7d
                        - 30d
                        - 90d
                        - 1y
                        - 2y
                        type: string
                    type: object
                  performance:
                    description: Performance configures performance optimizations
                    properties:
                      cacheBoost:
                        default: true
                        description: CacheBoost enables extended caching
                        type: boolean
                      cnameFlattening:
                        default: true
                        description: CNAMEFlattening enables CNAME flattening
                        type: boolean
                      ecs:
                        default: true
                        description: ECS enables EDNS Client Subnet
                        type: boolean
                    type: object
                  web3:
                    default: false
                    description: Web3 enables Web3 domain resolution
                    type: boolean
                type: object
              tldListRefs:
                description: |-
                  TLDListRefs references NextDNSTLDList resources
                  TLDs from all referenced lists are merged
                items:
                  description: ListReference references a list CRD (allowlist, denylist,
                    or TLD list)
                  properties:
                    name:
                      description: Name of the list resource
                      type: string
                    namespace:
                      description: Namespace of the list resource (defaults to profile's
                        namespace)
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - credentialsRef
            type: object
          status:
            description: NextDNSProfileStatus defines the observed state of NextDNSProfile
            properties:
              aggregatedCounts:
                description: AggregatedCounts tracks totals from all sources
                properties:
                  allowlistDomains:
                    description: AllowlistDomains is the total count of allowlisted
                      domains
                    type: integer
                  blockedTLDs:
                    description: BlockedTLDs is the total count of blocked TLDs
                    type: integer
                  denylistDomains:
                    description: DenylistDomains is the total count of denylisted
                      domains
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              fingerprint:
                description: Fingerprint is the unique profile configuration fingerprint
                  from the NextDNS API
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the profile was synced
                  with NextDNS
                format: date-time
                type: string
              observedConfig:
                description: |-
                  ObservedConfig contains the full observed state of the remote profile
                  Populated in observe mode; cleared after first successful managed sync
                properties:
                  allowlist:
                    description: Allowlist contains observed allowed domains
                    items:
                      description: ObservedDomainEntry represents an observed domain
                        with active state
                      properties:
                        active:
                          type: boolean
                        domain:
                          type: string
                      required:
                      - active
                      - domain
                      type: object
                    type: array
                  blockedTLDs:
                    description: BlockedTLDs contains observed blocked TLDs
                    items:
                      type: string
                    type: array
                  denylist:
                    description: Denylist contains observed denied domains
                    items:
                      description: ObservedDomainEntry represents an observed domain
                        with active state
                      properties:
                        active:
                          type: boolean
                        domain:
                          type: string
                      required:
                      - active
                      - domain
                      type: object
                    type: array
                  name:
                    description: Name is the profile name as shown in NextDNS dashboard
                    type: string
                  parentalControl:
                    description: ParentalControl contains observed parental control
                      settings
                    properties:
                      blockBypass:
                        type: boolean
                      categories:
                        items:
                          description: ObservedCategoryEntry represents an observed
                            content category
                          properties:
                            active:
                              type: boolean
                            id:
                              type: string
                            recreation:
                              type: boolean
                          required:
                          - active
                          - id
                          - recreation
                          type: object
                        type: array
                      recreation:
                        description: ObservedRecreation represents observed recreation
                          schedule settings
                        properties:
                          times:
                            description: ObservedRecreationTimes represents the days
                              of the week with recreation intervals
                            properties:
                              friday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                              monday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                              saturday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                              sunday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                              thursday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                              tuesday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                              wednesday:
                                description: ObservedRecreationInterval represents
                                  a start and end time for recreation
                                properties:
                                  end:
                                    type: string
                                  start:
                                    type: string
                                required:
                                - end
                                - start
                                type: object
                            type: object
                          timezone:
                            type: string
                        type: object
                      safeSearch:
                        type: boolean
                      services:
                        items:
                          description: ObservedServiceEntry represents an observed
                            blocked service
                          properties:
                            active:
                              type: boolean
                            id:
                              type: string
                          required:
                          - active
                          - id
                          type: object
                        type: array
                      youtubeRestrictedMode:
                        type: boolean
                    required:
                    - blockBypass
                    - safeSearch
                    - youtubeRestrictedMode
                    type: object
                  privacy:
                    description: Privacy contains observed privacy settings
                    properties:
                      allowAffiliate:
                        type: boolean
                      blocklists:
                        items:
                          description: ObservedBlocklistEntry represents an observed
                            privacy blocklist
                          properties:
                            id:
                              type: string
                          required:
                          - id
                          type: object
                        type: array
                      disguisedTrackers:
                        type: boolean
                      natives:
                        items:
                          description: ObservedNativeEntry represents an observed
                            native tracker protection
                          properties:
                            id:
                              type: string
                          required:
                          - id
                          type: object
                        type: array
                    required:
                    - allowAffiliate
                    - disguisedTrackers
                    type: object
                  rewrites:
                    description: Rewrites contains observed DNS rewrites
                    items:
                      description: ObservedRewriteEntry represents an observed DNS
                        rewrite
                      properties:
                        content:
                          type: string
                        name:
                          type: string
                      required:
                      - content
                      - name
                      type: object
                    type: array
                  security:
                    description: Security contains observed security settings
                    properties:
                      aiThreatDetection:
                        type: boolean
                      cryptojacking:
                        type: boolean
                      csam:
                        type: boolean
                      ddns:
                        type: boolean
                      dga:
                        type: boolean
                      dnsRebinding:
                        type: boolean
                      googleSafeBrowsing:
                        type: boolean
                      idnHomographs:
                        type: boolean
                      nrd:
                        type: boolean
                      parking:
                        type: boolean
                      threatIntelligenceFeeds:
                        type: boolean
                      typosquatting:
                        type: boolean
                    required:
                    - aiThreatDetection
                    - cryptojacking
                    - csam
                    - ddns
                    - dga
                    - dnsRebinding
                    - googleSafeBrowsing
                    - idnHomographs
                    - nrd
                    - parking
                    - threatIntelligenceFeeds
                    - typosquatting
                    type: object
                  settings:
                    description: Settings contains observed general settings
                    properties:
                      blockPage:
                        description: ObservedBlockPage represents observed block page
                          settings
                        properties:
                          enabled:
                            type: boolean
                        required:
                        - enabled
                        type: object
                      logs:
                        description: ObservedLogs represents observed logging settings
                        properties:
                          enabled:
                            type: boolean
                          location:
                            description: Location is the log storage location (e.g.,
                              "eu", "us", "ch")
                            type: string
                          logClientsIPs:
                            description: |-
                              LogClientsIPs indicates whether client IPs are logged.
                              Derived from the API's Drop.IP field (inverted: LogClientsIPs = !Drop.IP).
                            type: boolean
                          logDomains:
                            description: |-
                              LogDomains indicates whether queried domains are logged.
                              Derived from the API's Drop.Domain field (inverted: LogDomains = !Drop.Domain).
                            type: boolean
                          retention:
                            type: integer
                        required:
                        - enabled
                        - logClientsIPs
                        - logDomains
                        type: object
                      performance:
                        description: ObservedPerformance represents observed performance
                          settings
                        properties:
                          cacheBoost:
                            type: boolean
                          cnameFlattening:
                            type: boolean
                          ecs:
                            type: boolean
                        required:
                        - cacheBoost
                        - cnameFlattening
                        - ecs
                        type: object
                      web3:
                        type: boolean
                    required:
                    - web3
                    type: object
                  setup:
                    description: |-
                      Setup contains observed DNS setup/endpoint configuration
                      This is read-only data from the API — not user-configurable via spec
                    properties:
                      dnscrypt:
                        description: DNSCrypt contains the DNSCrypt protocol stamp
                        type: string
                      ipv4:
                        description: IPv4 contains DNS-over-HTTPS IPv4 addresses
                        items:
                          type: string
                        type: array
                      ipv6:
                        description: IPv6 contains DNS-over-HTTPS IPv6 addresses
                        items:
                          type: string
                        type: array
                      linkedIP:
                        description: LinkedIP contains linked IP configuration
                        properties:
                          ddns:
                            description: DDNS is the dynamic DNS hostname
                            type: string
                          ip:
                            description: IP is the currently linked IP address
                            type: string
                          servers:
                            description: Servers contains the linked IP DNS server
                              addresses
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the controller
                format: int64
                type: integer
              profileID:
                description: ProfileID is the NextDNS-assigned profile identifier
                type: string
              referencedResources:
                description: ReferencedResources tracks the status of referenced resources
                properties:
                  allowlists:
                    description: Allowlists lists the status of referenced allowlists
                    items:
                      description: ReferencedResourceStatus tracks the status of a
                        referenced resource
                      properties:
                        count:
                          description: Count of items (domains or TLDs)
                          type: integer
                        name:
                          description: Name of the resource
                          type: string
                        namespace:
                          description: Namespace of the resource
                          type: string
                        ready:
                          description: Ready indicates if the resource is ready
                          type: boolean
                      required:
                      - name
                      - namespace
                      - ready
                      type: object
                    type: array
                  denylists:
                    description: Denylists lists the status of referenced denylists
                    items:
                      description: ReferencedResourceStatus tracks the status of a
                        referenced resource
                      properties:
                        count:
                          description: Count of items (domains or TLDs)
                          type: integer
                        name:
                          description: Name of the resource
                          type: string
                        namespace:
                          description: Namespace of the resource
                          type: string
                        ready:
                          description: Ready indicates if the resource is ready
                          type: boolean
                      required:
                      - name
                      - namespace
                      - ready
                      type: object
                    type: array
                  tldLists:
                    description: TLDLists lists the status of referenced TLD lists
                    items:
                      description: ReferencedResourceStatus tracks the status of a
                        referenced resource
                      properties:
                        count:
                          description: Count of items (domains or TLDs)
                          type: integer
                        name:
                          description: Name of the resource
                          type: string
                        namespace:
                          description: Namespace of the resource
                          type: string
                        ready:
                          description: Ready indicates if the resource is ready
                          type: boolean
                      required:
                      - name
                      - namespace
                      - ready
                      type: object
                    type: array
                type: object
              suggestedSpec:
                description: |-
                  SuggestedSpec contains a spec-compatible translation of the observed config
                  Copy fields from this into spec when transitioning from observe to managed mode
                  Populated in observe mode; cleared after first successful managed sync
                properties:
                  allowlist:
                    description: Allowlist contains suggested allowed domains
                    items:
                      description: DomainEntry represents a domain in allow/deny lists
                      properties:
                        active:
                          default: true
                          description: Active indicates if this entry is enabled
                          type: boolean
                        domain:
                          description: Domain is the domain name (supports wildcards
                            like *.example.com)
                          maxLength: 253
                          minLength: 1
                          pattern: ^(\*\.)?([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                          type: string
                        reason:
                          description: Reason documents why this domain is in the
                            list
                          type: string
                      required:
                      - domain
                      type: object
                    type: array
                  blockedTLDs:
                    description: |-
                      BlockedTLDs lists TLDs from the remote profile
                      These must be placed in a NextDNSTLDList CR and referenced via spec.tldListRefs
                    items:
                      type: string
                    type: array
                  denylist:
                    description: Denylist contains suggested denied domains
                    items:
                      description: DomainEntry represents a domain in allow/deny lists
                      properties:
                        active:
                          default: true
                          description: Active indicates if this entry is enabled
                          type: boolean
                        domain:
                          description: Domain is the domain name (supports wildcards
                            like *.example.com)
                          maxLength: 253
                          minLength: 1
                          pattern: ^(\*\.)?([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                          type: string
                        reason:
                          description: Reason documents why this domain is in the
                            list
                          type: string
                      required:
                      - domain
                      type: object
                    type: array
                  name:
                    description: Name is the profile name
                    type: string
                  parentalControl:
                    description: ParentalControl contains suggested parental control
                      settings
                    properties:
                      blockBypass:
                        default: false
                        description: BlockBypass prevents bypassing parental controls
                        type: boolean
                      categories:
                        description: Categories specifies content categories to block
                        items:
                          description: CategoryEntry references a content category
                          properties:
                            active:
                              default: true
                              description: Active indicates if this category is blocked
                              type: boolean
                            id:
                              description: ID is the category identifier (e.g., "gambling",
                                "adult", "violence")
                              type: string
                            recreation:
                              default: false
                              description: |-
                                Recreation indicates if this category allows recreation time exceptions.
                                Note: Observe mode reads this from the API. Managed mode write support is deferred.
                              type: boolean
                          required:
                          - id
                          type: object
                        type: array
                      safeSearch:
                        default: false
                        description: SafeSearch enforces safe search on search engines
                        type: boolean
                      services:
                        description: Services specifies specific services to block
                        items:
                          description: ServiceEntry references a specific service
                          properties:
                            active:
                              default: true
                              description: Active indicates if this service is blocked
                              type: boolean
                            id:
                              description: ID is the service identifier (e.g., "tiktok",
                                "youtube", "facebook")
                              type: string
                          required:
                          - id
                          type: object
                        type: array
                      youtubeRestrictedMode:
                        default: false
                        description: YouTubeRestrictedMode enforces YouTube restricted
                          mode
                        type: boolean
                    type: object
                  privacy:
                    description: Privacy contains suggested privacy settings
                    properties:
                      allowAffiliate:
                        default: false
                        description: AllowAffiliate allows affiliate & tracking links
                        type: boolean
                      blocklists:
                        description: Blocklists specifies which ad/tracker blocklists
                          to enable
                        items:
                          description: BlocklistEntry references a privacy blocklist
                          properties:
                            active:
                              default: true
                              description: Active indicates if this blocklist is enabled
                              type: boolean
                            id:
                              description: ID is the blocklist identifier (e.g., "nextdns-recommended",
                                "oisd")
                              type: string
                          required:
                          - id
                          type: object
                        type: array
                      disguisedTrackers:
                        default: true
                        description: DisguisedTrackers blocks trackers using CNAME
                          cloaking
                        type: boolean
                      natives:
                        description: Natives specifies native tracking protection
                          (per-vendor)
                        items:
                          description: NativeEntry configures native tracker blocking
                            for a vendor
                          properties:
                            active:
                              default: true
                              description: Active indicates if blocking is enabled
                                for this vendor
                              type: boolean
                            id:
                              description: ID is the vendor identifier (e.g., "apple",
                                "windows", "samsung")
                              type: string
                          required:
                          - id
                          type: object
                        type: array
                    type: object
                  rewrites:
                    description: Rewrites contains suggested DNS rewrites
                    items:
                      description: RewriteEntry defines a DNS rewrite rule
                      properties:
                        active:
                          default: true
                          description: Active indicates if this rewrite is enabled
                          type: boolean
                        from:
                          description: From is the source domain
                          type: string
                        to:
                          description: To is the target (IP or domain)
                          type: string
                      required:
                      - from
                      - to
                      type: object
                    type: array
                  security:
                    description: Security contains suggested security settings
                    properties:
                      aiThreatDetection:
                        default: true
                        description: AIThreatDetection enables AI-based threat detection
                        type: boolean
                      cryptojacking:
                        default: true
                        description: Cryptojacking blocks cryptomining scripts
                        type: boolean
                      csam:
                        default: true
                        description: CSAM blocks child sexual abuse material
                        type: boolean
                      ddns:
                        default: false
                        description: DDNS blocks dynamic DNS hostnames
                        type: boolean
                      dga:
                        default: true
                        description: DGA blocks algorithmically-generated domains
                        type: boolean
                      dnsRebinding:
                        default: true
                        description: DNSRebinding protects against DNS rebinding attacks
                        type: boolean
                      googleSafeBrowsing:
                        default: true
                        description: GoogleSafeBrowsing enables Google Safe Browsing
                          protection
                        type: boolean
                      idnHomographs:
                        default: true
                        description: IDNHomographs blocks IDN homograph attacks
                        type: boolean
                      nrd:
                        default: false
                        description: NRD blocks newly registered domains
                        type: boolean
                      parking:
                        default: true
                        description: Parking blocks parked domains
                        type: boolean
                      threatIntelligenceFeeds:
                        default: true
                        description: ThreatIntelligenceFeeds enables threat intelligence
                          feeds
                        type: boolean
                      typosquatting:
                        default: true
                        description: Typosquatting blocks typosquatting domains
                        type: boolean
                    type: object
                  settings:
                    description: Settings contains suggested general settings
                    properties:
                      blockPage:
                        description: BlockPage configures the block page
                        properties:
                          enabled:
                            default: true
                            description: Enabled shows a block page instead of failing
                              silently
                            type: boolean
                        type: object
                      logs:
                        description: Logs configures query logging
                        properties:
                          enabled:
                            default: true
                            description: Enabled turns logging on/off
                            type: boolean
                          location:
                            description: |-
                              Location specifies the log storage location (e.g., "eu", "us", "ch").
                              Valid values depend on the NextDNS plan and may change over time.
                            type: string
                          logClientsIPs:
                            default: false
                            description: LogClientsIPs logs client IP addresses
                            type: boolean
                          logDomains:
                            default: true
                            description: LogDomains logs queried domains
                            type: boolean
                          retention:
                            default: 7d
                            description: Retention specifies log retention period
                            enum:
                            - 1h
                            - 6h
                            - 1d
                            - 7d
                            - 30d
                            - 90d
                            - 1y
                            - 2y
                            type: string
                        type: object
                      performance:
                        description: Performance configures performance optimizations
                        properties:
                          cacheBoost:
                            default: true
                            description: CacheBoost enables extended caching
                            type: boolean
                          cnameFlattening:
                            default: true
                            description: CNAMEFlattening enables CNAME flattening
                            type: boolean
                          ecs:
                            default: true
                            description: ECS enables EDNS Client Subnet
                            type: boolean
                        type: object
                      web3:
                        default: false
                        description: Web3 enables Web3 domain resolution
                        type: boolean
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
